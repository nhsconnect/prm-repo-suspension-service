format_version: 4
common:
  plan_job: &plan_job
    resources:
      - docker
    artifacts:
      - build:
          source: terraform/nhs_deployment.tfplan
          destination: terraform/
    tasks:
      - exec:
          command: /bin/bash
          arguments:
            - -c
            - ./tasks tf_plan create
  deploy_job: &deploy_job
    resources:
      - docker
    tasks:
      - fetch:
          stage: plan
          job: plan
          source: terraform/nhs_deployment.tfplan
          destination: terraform/
          is_file: yes
      - exec:
          command: /bin/bash
          arguments:
            - -c
            - ./tasks tf_apply
      - exec:
          command: /bin/bash
          arguments:
            - -c
            - ./tasks wait_ecs
  common_stages: &common_stages
    - promote_docker_image:
        resources:
          - docker
        clean_workspace: true
        tasks:
          - exec:
              command: /bin/bash
              arguments:
                - -c
                - ./tasks promote_docker_image
    - plan:
        clean_workspace: true
        jobs:
          plan: *plan_job
    - deploy:
        clean_workspace: true
        jobs:
          deploy: *deploy_job
  prod_stages: &prod_stages
    - promote_docker_image:
        resources:
          - docker
        clean_workspace: true
        tasks:
          - exec:
              command: /bin/bash
              arguments:
                - -c
                - ./tasks promote_docker_image
    - plan:
        clean_workspace: true
        jobs:
          plan: *plan_job
    - deploy:
        approval:
          type: manual
          allow_only_on_success: true
        clean_workspace: true
        jobs:
          deploy: *deploy_job
pipelines:
  'prm-repo-suspension-service.dev':
    group: continuity-service
    label_template: '${git[:8]}'
    materials:
      git:
        type: configrepo
      app:
        pipeline: prm-repo-suspension-service
        stage: docker
      base_infra:
        pipeline: prm-deductions-infra
        stage: deploy_dev
    environment_variables:
      NHS_ENVIRONMENT: dev
    stages: *common_stages

  'prm-repo-suspension-service.test':
    group: continuity-service
    label_template: '${git[:8]}'
    materials:
      git:
        type: configrepo
      app:
        pipeline: prm-repo-suspension-service
        stage: docker
      base_infra:
        pipeline: prm-deductions-infra
        stage: deploy_test
      dev_env:
        pipeline: prm-repo-suspension-service.dev
        stage: deploy
    environment_variables:
      NHS_ENVIRONMENT: test
    stages: *common_stages

  'prm-repo-suspension-service.pre-prod':
    group: continuity-service
    label_template: '${git[:8]}'
    materials:
      git:
        type: configrepo
      app:
        pipeline: prm-repo-suspension-service
        stage: docker
      base_infra:
        pipeline: prm-deductions-infra
        stage: deploy_pre_prod
      test_env:
        pipeline: prm-repo-suspension-service.test
        stage: deploy
    environment_variables:
      NHS_ENVIRONMENT: pre-prod
    stages: *common_stages

  'prm-repo-suspension-service.prod':
    group: continuity-service
    label_template: '${git[:8]}'
    materials:
      git:
        type: configrepo
      app:
        pipeline: prm-repo-suspension-service
        stage: docker
      base_infra:
        pipeline: prm-deductions-infra
        stage: deploy_prod
      test_env:
        pipeline: prm-repo-suspension-service.pre-prod
        stage: deploy
    environment_variables:
      NHS_ENVIRONMENT: prod
    stages: *prod_stages
